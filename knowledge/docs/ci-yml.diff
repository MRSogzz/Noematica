--- /tmp/orig_extract/Noematica-main/.github/workflows/ci.yml	2026-07-26 14:24:56.000000000 +0000
+++ /home/claude/Noematica-main/.github/workflows/ci.yml	2026-07-28 04:48:32.225373610 +0000
@@ -107,7 +107,10 @@
         run: |
           echo "Measuring real I/O latency for changed modules..."
           npm run ci-writeback -- \
-            --module-id="${{ github.event.head_commit.message }}" \
+            --detect-changed \
+            --base="${{ github.event.before }}" \
+            --head="${{ github.sha }}" \
+            --status=DONE \
             --commit-sha="${{ github.sha }}"
         env:
           MODULES_DIR:   ./modules
