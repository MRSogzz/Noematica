/* ============================================================
   shell/toolbar.js — WorkspaceShell 的 Toolbar 層
   ============================================================
   Toolbar 目前的「共用」程度比 Sidebar/InfoPanel 低很多——每個面板的
   工具列內容差異很大（F1 是檔名輸入框+存檔鈕，B 是搜尋框+Pipeline
   狀態列，F5 是搜尋框+文件數量），共用的只有「這塊 DOM 容器歸誰管」
   這件事，不是排版邏輯本身。各面板的 view.js 自己組好完整 HTML 字串
   （例如 toolbarHtml()），這裡只負責塞進去。

   如果之後 Toolbar 也要發展出跟 Sidebar 一樣的「共用行為」（不只是
   共用容器），例如所有面板的工具列都固定是「搜尋框 + 靠右一顆主要
   按鈕」，才需要把這個函式的介面改成跟 renderSidebar() 一樣吃結構化
   資料，不是吃 HTML 字串——目前先不做這個假設。
   ============================================================ */

function renderToolbar(html) { $('m-toolbar').innerHTML = html; }
